# Example environment variables for Backend
# Replace values before running in production

# Database connection (full DATABASE_URL expected by Prisma)
# Example: postgresql://kryptovault:super-secret-password@localhost:5432/kryptovault
DATABASE_URL=postgresql://kryptovault:super-secret-password@postgres:5432/kryptovault

# JWT signing secret (use a securely generated long secret in production)
JWT_SECRET=replace_with_a_secure_random_string

# Application port
PORT=3000

# Environment: development | production | test
NODE_ENV=development

# Frontend origin allowed for CORS (optional)
CORS_ORIGIN=http://localhost:5173

# If you prefer using DB_PASSWORD separately, Docker Compose sets DATABASE_URL for you using DB_PASSWORD
# DB_PASSWORD=super-secret-password
